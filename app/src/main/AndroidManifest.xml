<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ejercicio11"
        tools:targetApi="31">
        <activity
            android:name=".Enemigo2Activity"
            android:exported="false" />
        <activity
            android:name=".RecogerobjetoActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EligerazaActivity" />
        <activity android:name=".BlancaActivity" />
        <activity android:name=".CiudadActivity" />
        <activity android:name=".DadoActivity" />
        <activity android:name=".EligeclaseActivity" />
        <activity android:name=".EnemigoActivity" />
        <activity android:name=".MercaderActivity" />
        <activity android:name=".ObjetoActivity" />
        <activity android:name=".ResumenActivity" />
    </application>

</manifest>